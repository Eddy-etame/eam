import { NotFoundScreen } from '@/components/layout/NotFoundScreen'

/** Locale-tree 404 boundary — see NotFoundScreen for why it is server-rendered. */
export default function NotFound() {
  return <NotFoundScreen />
}
